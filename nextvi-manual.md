# Nextvi(1) General Commands Manual

<details><summary>NAME<\summary>

Nextvi - 소형 vi/ex 터미널 텍스트 편집기

<\details>


<details><summary>SYNOPSIS<\summary>

```
vi [-aemsv] [file ...]
```

<\details>


<details><summary>DESCRIPTION<\summary>

`Nextvi` 는 1976년 빌 조이(*Bill Joy*)가 유닉스 기반 시스템을 위해 처음 개발한 명령줄 텍스트 편집기 `vi(1)` 의 현대적인 클론입니다.

Nextvi는 vi(1)의 다양한 표준 기능을 기반으로 처리하며 사용자의 효율적인 텍스트 편집을 위해 일반 모드(*normal mode*), 삽입 모드(*insert mode*), 명령 모드(*ex mode*)로 전환할 수 있는 독특한 모달 인터페이스가 포함됩니다. 추가 개선 사항은 제한없는 매크로 시스템, 구문 강조, 키맵, 양방향 UTF-8 지원 및 그 밖의 수많은 기능이 포함됩니다. 

Nextvi는 매우 효율적이고 이식성이 뛰어나며 사용자 맞춤형으로 수정하기 쉽고 앞으로도 오랫동안 그 중요성과 높은 품질을 그대로 유지할 것입니다.

<\details>


<details><summary>OPTIONS<\summary>

```
-a      대체 터미널 화면 활성화
-e      시작 시 ex 모드 진입(구문 강조 등)
-m      초기 파일 읽기 메시지 비활성화
-s      시작 시 원시 ex 모드 진입(일반 텍스트)
-v      모든 옵션 해제
```

<\details>



<details><summary>MANPAGE NOTATION<\summary>

```
<x>     x가 문자 리터럴임을 의미하는 클로저
[x]     x가 선택적 인자임을 의미하는 클로저
{x}     x가 필수 인자임을 의미하는 클로저
“x”     x가 문자열임을 의미하는 클로저
<^X>    Ctrl 키와 X를 의미함
< >     클로저에서 대안을 구분하는 공백
#       클로저 내의 10진수 표현
x-y     x부터 y까지 범위
```

<\details>



<details><summary>VI NORMAL<\summary>

```
[#]j          # 줄 아래로 이동
[#]k          # 줄 위로 이동
[#]+
[#]<^M>
[#]<Enter>    # 줄 아래로 이동, 커서를 들여쓰기 뒤 위치로 이동
[#]-          # 줄 위로 이동, 커서를 들여쓰기 뒤 위치로 이동
[#]h          # 열 왼쪽으로 이동
[#]l          # 열 오른쪽으로 이동
[#]f{arg}     arg 문자가 나타나는 지점까지 앞으로 #번 이동
[#]F{arg}     arg 문자가 나타나는 지점까지 뒤로 #번 이동
[#]t{arg}     arg 문자가 나타나는 지점까지 앞으로 #번 이동
[#]T{arg}     arg 문자가 나타나는 지점까지 뒤로 #번 이동
[#],          마지막 <f F t T> 이동을 #번 뒤로 반복
[#];          마지막 <f F t T> 이동을 #번 앞으로 반복
[#]E          단어 끝까지 #번 이동, 구두점 건너뛰기
[#]e          단어 끝까지 #번 이동
[#]B          단어 시작 부분까지 #번 뒤로 이동, 구두점 건너뛰기
[#]b          단어 시작 부분으로 #번 뒤로 이동
[#]W          단어 시작 부분으로 #번 앞으로 이동, 구두점 건너뛰기
[#]w          단어 시작 부분으로 #번 앞으로 이동
vw            <E e B b W w 공백 백스페이스> 영역에 단일 행 모드 전환
[#](          다음 문장 경계까지 #번 아래로 이동
[#])          다음 문장 경계까지 #번 위로 이동
[#]{          다음 <{> 섹션까지 #번 아래로 이동
[#]}          다음 <{> 섹션까지 #번 위로 이동
[#][          다음 <Newline> 섹션까지 #번 아래로 이동
[#]]          다음 <Newline> 섹션까지 #번 위로 이동
^             들여쓰기 후 줄 시작 부분으로 이동
0             줄 시작 부분으로 이동
$             줄 끝으로 이동
[#]|          # 열로 이동
[#]<스페이스>   # 글자만큼 앞으로 이동
[#]<^H>
[#]<백스페이스> # 글자만큼 뒤로 이동
%             가장 가까운 <] ) } [ ( {> 쌍으로 이동
{#}%          줄 번호의 # 퍼센트 지점으로 이동
'{<mark>}     줄 마크로 이동
`{<mark>}     커서 위치의 줄 마크로 이동
gg            버퍼 첫 번째 줄로 이동
[#]G          버퍼 마지막 줄 또는 #번째 줄로 이동
H             화면 가장 위쪽 줄로 이동
L             화면 가장 아래쪽 줄로 이동
M             화면 중간 줄로 이동
z.            커서 위치 기준으로 화면 중앙 정렬
z<^M>
z<Enter>      화면을 맨 위 행에 맞춰 중앙 정렬
z-            화면을 맨 아래 행에 맞춰 중앙 정렬
[#]<^E>       1줄 또는 #줄 아래로 스크롤하며 #과 커서 위치 유지
[#]<^Y>       1줄 또는 #줄 위로 스크롤하며 #과 커서 위치 유지
[#]<^D>       화면 크기 절반만큼 아래로 스크롤. [#] 인 경우 스크롤을 #줄로 설정
[#]<^U>       화면 크기 절반만큼 위로 스크롤. [#] 인 경우 #줄만큼 스크롤하도록 설정
<^B>          화면 크기만큼 위로 스크롤
<^F>          화면 크기만큼 아래로 스크롤
#             전역 및 상대 행 번호 표시. 일시적
2#            전역 행 번호 표시를 영구적으로 토글
4#            들여쓰기 후 상대 행 번호 표시를 영구적으로 토글
8#            상대 행 번호 표시를 영구적으로 토글
V             숨겨진 문자 표시를 토글: <공백 탭 줄바꿈>
<^C>          <l h e b E B w W> 에 대한 행 이동 번호 표시 토글
{<1-5>}<^C>   행 이동 번호 모드 1-5를 선택
<^V>          줄 이동 번호 모드 순환
[#]<^R>       #번 재실행
[#]u          #번 실행 취소
<^I>
<Tab>         커서 위치부터 줄 끝까지 경로의 파일 열기
<^K>          현재 버퍼를 파일에 쓰기. 두 번 시도 시 강제 쓰기
[#]<^W>{arg}  arg 영역의 들여쓰기를 #번 줄이기
[#]<{arg}     arg 영역의 왼쪽 여백을 #번 들여쓰기
[#]>{arg}     arg 영역의 오른쪽 여백을 #번 들여쓰기
[#]"{<arg>}{cmd} 레지스터 arg를 사용해서 일반 cmd 실행. #은 cmd에 적용
[#]"{<arg>}q  매크로 기록을 시작하고 rr ex 옵션을 arg 레지스터로 설정. #레지스터 내용 유지
q             매크로 녹화를 중지하고 rr ex 옵션을 0으로 설정
R             레지스터와 내용 출력
[#]&{<arg>}   arg 레지스터 매크로를 비차단 모드로 #번 실행
[#]@{<arg>}   arg 레지스터 매크로를 차단 모드로 #번 실행
[#]@@         마지막 매크로를 차단 모드로 #번 실행
[#]&&         마지막 매크로를 비차단 모드로 #번 실행
[#]@:
[#]&:         <:> 레지스터를 ex 명령어로 #번 실행
[#].          마지막 일반 명령어를 #번 반복. 반복 가능한 명령어:
              x X D C s S Y p P r d c ~ gu gU g~ ! > < <^W> i I a A o O ci di J K
[#]v.         마지막 일반 명령을 # 줄 아래로 이동하며 반복
[#]Q          재귀적으로 ex 모드 진입. #은 현재 수평 스크롤 위치 유지
:             ex 프롬프트 진입
[#]!{arg}     # 또는 arg 영역을 기반으로 한 파이프 ex 프롬프트 진입
vv            히스토리 버퍼의 b-1번째 줄을 포함한 ex 프롬프트 진입
[#]vr         %s/ ex 프롬프트 진입. 커서 위치부터 # 단어 삽입
[#]vt[#arg]   .,.+0s/ ex 프롬프트 진입. 커서 위치부터 # 줄 삽입하며 커서 위치부터 #arg 단어 삽입
[#]v/         %s/ ex 프롬프트 진입. 정규식 키워드 설정. 커서 위치부터 # 단어 삽입
v;            ! ex 프롬프트 진입
[#]vi         %s/ ex 프롬프트 진입. 공백을 탭으로 바꾸는 정규식 포함. #으로 탭 너비 조정
[#]vI         %s/ ex 프롬프트 입력. 탭을 공백으로 바꾸는 정규식 포함. #으로 탭 너비 조정
vo            줄 끝 공백과 <\r> 줄 끝 문자 제거
<^G>          버퍼 상태 정보 출력
1<^G>         상태 행 영구 활성화
2<^G>         상태 행 영구 비활성화
ga            문자 정보 표시
1ga           문자 정보 행 영구 활성화
2ga           문자 정보 행 영구 비활성화
[#]gw         줄을 # 열 제한까지 강제로 줄바꿈. 기본값: 80
[#]gq         버퍼를 # 열 제한으로 강제 줄바꿈. 기본값: 80
[#]g~{arg}    arg 영역의 대소문자 #번 전환
[#]gu{arg}    arg 영역을 소문자로 #번 전환
[#]gU{arg}    arg 영역을 대문자로 #번 전환
[#]~          앞쪽으로 #번 이동하며 대소문자 전환
i             삽입 모드 진입
I             들여쓰기 후 줄 시작 부분에 삽입 모드 진입
A             줄 끝에서 삽입 모드 진입
a             한글자 앞으로 이동한 위치에 삽입 모드 진입
o             아래에 새 줄을 만들고 삽입 모드 진입
O             위에 새 줄을 만들고 삽입 모드 진입
[#]s          # 문자 삭제 후 삽입 모드 진입
[#]S          # 줄 전체 삭제 후 삽입 모드 진입
[#]c{arg}     arg 영역 #번 삭제 후 삽입 모드 진입
C             커서 위치부터 줄 끝까지 삭제하고 삽입 모드 진입
[#]d{arg}     arg 영역 #번 삭제
D             커서 위치부터 줄 끝까지 삭제
[#]x          커서 위치부터 앞으로 #글자 삭제
[#]X          커서 위치부터 뒤로 #글자 삭제
[#]di{<arg>}  arg 쌍 안의 내용 삭제 후 #개 일치 항목 건너뜀
[#]ci{<arg>}  arg 쌍 안의 내용 삭제 후 삽입 모드 진입. #개 일치 항목 건너뜀
[#]r{arg}     커서 위치부터 앞으로 #자리 arg 대체
[#]K          한 줄 #번 분할
[#]J          #줄 병합
[#]y{arg}     arg 영역 #번 복사
[#]Y          #줄 복사
[#]p          기본 레지스터 내용을 현재 줄 아래 또는 커서 위치 뒤에 #번 붙여넣기
[#]P          기본 레지스터 내용을 현재 줄 위나 커서 앞 위치에 #번 붙여넣기
m{<mark>}     버퍼 로컬 줄 표시 설정
<^T>               글로벌 줄 표시 0 설정. 글로벌 표시는 항상 유효함
{<0 2 4 6 8>}<^T>  글로벌 줄 표시 # 설정
{<1 3 5 7 9>}<^T>  전역 줄 표시 #으로 전환
[#]<^7>{<0-9>}
[#]<^_>{<0-9>}
[#]<^/>{<0-9>} 버퍼 목록 표시 후 프롬프트가 나타나면 # 또는 0-9 인덱스에 따라 버퍼 전환
<^^>
<^6>          이전 버퍼로 전환
[#]<^N>       다음 버퍼로 전환. #을 입력하면 앞뒤 방향 반전
\             /fm/ 디렉터리의 b-2 버퍼 전환
{#}\          b-2 버퍼에서 보조 디렉터리 목록 새로고침
vb            b-1 히스토리 버퍼로 재귀 진입. 종료 시 현재 줄을 ex 프롬프트에 삽입
z1            대체 키맵을 페르시아어 키맵으로 설정
z2            대체 키맵을 러시아어 키맵으로 설정
ze            영어 키맵으로 전환
zf            대체 키맵으로 전환
zL            td ex 옵션 2로 설정
zl            td ex 옵션 1로 설정
zr            td ex 옵션 -1로 설정
zR            td ex 옵션 -2로 설정
[#]/          정규식 검색 프롬프트, 1개 또는 #개의 일치 항목만큼 아래로 이동
[#]?          정규식 검색 프롬프트, 1개 또는 #개의 일치 항목만큼 위로 이동
[#]n          정규식 검색 반복, 1개 또는 #개의 일치 항목만큼 이동
[#]N          정규식 검색 반복, 1개 또는 #개의 일치 항목만큼 반대 방향으로 이동
<^A>          커서 위치에서 한 단어 분량 정규식 검색. 중심 위치를 무시하고 위나 아래로 순환 이동
*             
{#}*          정규식 검색, 중심 위치를 무시하고 위나 아래로 순환 이동
{#}<^A>       정규식 검색, 키워드를 커서 위치에서 #단어 분량으로 설정
<^]>          b-2 버퍼의 디렉터리 목록 기반으로 파일 시스템 정규식 검색을 앞으로 수행. <^P> 대체 기능을 위해 전역 줄 표시를 0으로 설정
{#}<^]>       파일 시스템 정규식 검색을 앞으로 수행, 키워드를 커서부터 # 단어 거리로 설정
[#]<^P>       파일 시스템 정규식 검색을 뒤로 수행
<^Z>          vi 일시 중지
<^L>          전체 화면 강제 다시 그리기 및 터미널 크기 업데이트
[#]Z{<Any>}   강제 종료 및 터미널 정리. # 재귀 레벨 종료
[#]Zz         강제 종료 및 히스토리 명령어 실행. # 재귀 레벨 종료
ZZ            종료 및 저장하지 않은 변경 사항 파일에 기록
<ESC ^C>      보류 중인 입력 또는 명령어 취소
```

<\details>


<details><summary>VI REGIONS<\summary>

영역은 vi 이동 명령의 [h v] 범위를 정의하는 vi 표준입니다. “move” 는 단어가 포함된 영역을 정의합니다.

```
j + <^M> <Enter> - k h l f F t T , ; B E b e W w ( ) { } [ ] ^ 0 $
        <Space> <^H> <Backspace> % ' ` G H L M / ? n N * <^A>

        All regions
```

<\details>


<details><summary>VI MOTIONS<\summary>

이동 명령은 [h v] 범위 내에서 실행되는 vi의 일반 명령입니다. “region” 이란 단어로 설명된 명령은 영역을 소비합니다. 이동 명령 앞이나 뒤에 [#] 을 붙일 수 있습니다.

```
<^W> > < ! c d y g~ gu gU
        모든 이동 명령

dd yy cc g~~ guu gUU >> << <^W><^W> !!
        [#]를 줄 수로 사용할 수 있는 특수 이동 명령
```

**예시:**  
```
3d/int  “int” 키워드가 세 번째로 나타나는 지점까지 텍스트 삭제
3dw     단어 3개 삭제 (접두사 [#])
d3w     단어 3개 삭제 (접미사 [#])
"ayl    <a> 레지스터에 문자 하나 복사
"Ayw    <a> 레지스터에 단어 하나 추가
```

<\details>


<details><summary>VI/EX INSERT<\summary>

```
<^H>
<Backspace>   문자 하나 삭제 후 빈 상태일 경우 ex 모드 초기화
<^U>          <^X> 표시까지 또는 전체 삭제
<^W>          단어 하나 삭제
<^T>          들여쓰기 늘림
<^D>          들여쓰기 줄임
<^]>          0~9번 레지스터 중에서 붙여넣기 레지스터를 순차적으로 선택
<^\>{<arg>}   붙여넣기 레지스터 arg 선택. <^\> 는 기본 레지스터 선택
<^P>          레지스터 내용 붙여넣기
<^X>          자동 완성 마크와 <^U> 시작 위치 설정. <^X> 는 마크 초기화
<^G>          자동 완성을 위해 현재 버퍼 색인화
<^Y>          색인화된 모든 자동 완성 데이터 초기화
<^R>          자동 완성 옵션 뒤로 순회
<^N>          자동 완성 옵션 앞으로 순회
<^B>          vi 삽입 모드일 때 자동 완성 옵션 출력
<^B>          ex 프롬프트에서 b-1 히스토리 버퍼 재귀 진입. 종료 시 현재 줄 ex 프롬프트에 삽입
<^A>          b-1 히스토리 버퍼 줄 순회
<^Z>          vi/ex 일시 중지
<^L>          vi 모드는 화면을 다시 그리며 ex 모드는 터미널 정리
<^O>          vi 모드와 ex 모드 간 재귀적 전환
<^E>          영어 키맵 전환
<^F>          대체 키맵 전환
<^V>{<arg>}   리터럴 문자 arg 읽기
<^K>{arg}     디그래프 시퀀스 arg 읽기
<^C>
<ESC>         vi 모드는 삽입 모드 종료, ex 모드는 초기화
<^M>
<Enter>       vi 모드는 <줄바꿈> 삽입, ex 모드는 명령 실행
```

<\details>


<details><summary>EX<\summary>

Ex는 유닉스 시스템의 강력한 라인 에디터로 1976년에 빌 조이(*Bill Joy*)가 처음 개발했습니다. 이 필수적인 도구는 vi의 핵심 역할을 처리하며 vi에서 명령어와 매크로를 실행할 수 있게 해줄 뿐만 아니라 필요할 때 순수 명령줄 인터페이스(CLI)로 전환할 수도 있게 해줍니다.

<\details>


<details><summary>EX PARSING<\summary>

구문 분석은 다음과 같은 구조를 따릅니다:

```
[<sep>][prefix][cmd][<pad>][args]
```

명령어는 `<sep>` 으로 시작하고 `<sep>` 으로 구분됩니다. 필드는 `<Space>` 또는 `<Tab>` 으로 패딩될 수 있습니다. [prefix] 는 [cmd] 가 소비하는 필드로 [range] 구조의 문자를 허용합니다. 중첩된 블록 내부를 제외하고 [prefix] 구조 내부의 패딩은 축소됩니다.

[cmd] 와 [args] 사이엔 패딩이 하나만 있을 수 있습니다. 스크립트에서 모호성을 피하기 위해 [cmd] 와 [args] 사이엔 항상 패딩을 사용하는 것이 좋습니다.

**예시:**  

```
:evi.c
        “ :e vi.c” 로 평가됩니다.
:efbc
        “ :ef bc” 로 평가되며 “ :e fbc” 로 평가되지 않습니다.
:e  vi.c
        “ vi.c” 를 편집합니다. <pad> 는 필수입니다.
```

<\details>


<details><summary>EX ESCAPES<\summary>

[args] 및 [prefix] 블록 내의 특수 문자는 `<\>` 로 이스케이프 처리하면 일반 문자로 변환됩니다. 이스케이프 처리는 POSIX와 다른 짝수/홀수 규칙이 적용됩니다. 즉, 특수 문자 바로 앞의 이스케이프는 절반으로 줄어들고 홀수 개일 경우 해당 특수 문자는 일반 문자로 변환되며 그 외 위치의 이스케이프는 리터럴로 처리됩니다.

```
( ^ ] -
        정규 표현식 “[]” 내의 특수 문자
( ) { + * ? ^ $ [ | \ . \< \>
        정규 표현식 내의 특수 문자
\ : % !
        ex 내의 특수 문자
```

<\details>


<details><summary>EX EXPANSION<\summary>

[args] 내의 `<%>` 는 현재 버퍼의 경로명이나 해당 버퍼 번호가 뒤따를 경우 어떤 버퍼의 경로명으로 확장됩니다.

%# 는 마지막으로 전환된 버퍼의 경로명으로 확장됩니다. %@ 는 지정된 레지스터로 확장됩니다.

```
예시: 현재 파일의 복사본을 만들고 편집하기
:!cp % %_:e %_

예시: 현재 버퍼의 경로명 삽입하기
:&i%

예: <a> 레지스터의 값 출력
:!echo %@97
```

[args] 내의 `<!>` 는 외부 명령을 포함한 블록을 시작하며 선택적으로 끝을 표시합니다. 이 블록은 실행되어 생성된 stdout 및 stderr로 확장됩니다.

```
예: “int”를 $RANDOM의 값으로 치환
:%s/int/!printf “%s” $RANDOM!

예: ls 셸 명령의 출력 삽입
:&i!ls

예: ls 출력을 더 효율적으로 삽입
:;c!ls!<^V><ESC>
```

<\details>


<details><summary>EX RANGES<\summary>

일부 ex 명령어 앞에 범위 지정자를 붙일 수 있습니다.

- [range] 접두사는 수직 및 수평 범위를 지정합니다.
- [vrange] 접두사는 수직 범위와 수평 위치를 지정합니다.
- [cmd] 가 없을 경우 [vrange] 가 [prefix] 를 대체합니다.

```
[% |][, ;][#][. $ ' > <][- + * / %][#num]
        모든 범위 구조
{|}{cmd}[|]
        Ex 하위 명령 구조
{>}[정규식][>]
        앞으로 검색 구조
{<}[정규식][<]
        뒤로 검색 구조
'{#mark}
        마크 구조

%       첫 번째 줄부터 마지막 줄까지 범위
|       ex 하위 명령 블록 시작
,       수직 범위 구분자
;       수평 범위 구분자
#       범위 구조에서 이전 값 재설정
.       현재 위치
$       버퍼 마지막 줄 또는 줄 끝
'       마크 구조 시작
>       앞으로 검색 블록 시작
<       뒤로 검색 블록 시작
-       뒤따르는 숫자를 뺌
+       뒤따르는 숫자를 더함
*       뒤따르는 숫자로 곱함
/       뒤따르는 숫자로 나눕
%       뒤따르는 숫자로 나눈 나머지
#num    숫자 또는 위치
```

**예시:** 
```
:1,5p   
        1, 5행 출력

:.-5,.+5p
        현재 위치 주변 5행 출력

:>int>p
        “int” 가 처음 나타나는 위치 출력

:<int<p
        “int” 가 처음 나타나는 위치 역순 출력

:.,>int>p
        “int” 가 발견될 때까지 출력

:<int<,.p
        “int” 가 발견될 때까지 역순 출력

:>      
       이전에 설정된 정규식 키워드를 사용해서 검색

:'100,'97p
       마크 <d> 부터 마크 <a> 까지 줄 출력

:%p     
       버퍼 모든 줄 출력

:$p     
       버퍼 마지막 줄 출력

:$*50/100+1
       파일 50% 지점 이동

:;50    
       50번째 문자 위치로 이동

:10;50  
       10번째 줄의 50번째 문자 위치로 이동

:10;.+5
        10번째 줄에서 5번째 문자 위치로 이동

:'97;'97
        <a> 마크의 50번째 오프셋 위치로 이동

:;$     
       줄 끝으로 이동

:5;>int>
       5번째 줄에서 “int” 검색

:.;<int<
       현재 줄에서 “int” 역방향 검색

:;5;+10=
        +10은 5가 아닌 초기 현재 오프셋 기준의 상대적 값

:;5;#+10=
        +10은 5 기준

:;>int>+3;#>>p
        현재 줄에서 “int” 로 둘러싸인 텍스트 출력

:|grp1|;>(a)+>+1|grp|;#>>p
        비결정적 패턴 “a+” 로 둘러싸인 텍스트 출력
```

<\details>


<details><summary>EX COMMANDS<\summary>

```
[range]f>[regex]
[vrange]f<[regex]
        범위 검색

        인수를 지정하지 않으면 이전에 설정한 정규식 키워드를 사용됩니다.
        대상을 찾지 못하면 오류를 반환합니다.
        커서가 범위 밖인 경우 앞으로 검색 시 커서를 범위 시작점으로
        뒤로 검색 시 커서를 범위 끝점으로 고정합니다.

        예시: 범위 지정 없음, 현재 줄만
        :f>int
		
        예시: 역방향
        :f<int
		
        예시: 범위 지정
        :10,100f>int

[range]f+[regex]
[vrange]f-[정규식]
        증분 범위 검색

        :f> 및 :f< 명령과 동일하지만 후속 명령은 일치 경계를 넘어 진행됩니다. 
		즉, 해당 위치에서 일치시킬 수 없습니다.

[vrange]f[정규식]
        퍼지 검색 프롬프트

        일치 항목 목록이 포함된 퍼지 검색 프롬프트로 들어갑니다.
        일치 항목 앞에는 선택 번호와 줄 번호가 표시됩니다.
        일치 항목이 10개 미만인 경우
        0~9의 숫자를 사용해서 일치 항목을 선택할 수 있습니다.
        <^C> 또는 <ESC> 를 누르면 중단됩니다.
        <^M> 또는 <Enter> 를 누르면 9보다 큰 선택 번호를 입력할 수 있습니다.

        범위가 지정되지 않으면 <%> 범위로 처리됩니다.
        범위가 지정되지 않은 경우 최대 xrows * 3개의 일치 항목이 표시됩니다.

[vrange]g[<Delim>][regex][<Delim>][cmd]
        전역 명령어

        정규 표현식과 일치하는 줄 범위에 ex 명령어를 실행합니다.
        패턴과 일치한 줄이 없거나 [cmd] 가 실패하면 오류를 반환합니다.

        중첩된 경우 범위 지정없이 <%> 범위 또는 <.> 범위로 평가됩니다.

        예시: 모든 빈 줄 제거
        :g/^$/d

        예시: 이전에 설정된 정규식 키워드와 일치한 줄 출력
        :g//p

        하나의 전역 명령어에서 여러 ex 명령어를 연쇄적으로 실행할 수 있습니다.
        명령어를 연쇄적으로 실행하려면 ex 구분자 <:> 을 한 번 이스케이프 처리해야 합니다.

        예: “int” 와 일치하는 줄을 출력하고 <a> 레지스터에 추가
        :g/int/p\:ya+ 97

        글로벌 명령어 내부에 다른 글로벌 명령어를 중첩할 수 있습니다.
        중첩된 글로벌 명령어에 의해 변경된 줄에는 첫 번째 글로벌 명령어가 실행되지 않습니다.

        예시: 중첩된 전역 명령
        “int” 가 포함되고 <;> 로 끝나는 모든 줄에 “has a semicolon” 을 추가합니다.
        :g/int/g/;$/& A has a semicolon

        예: “()” 로 묶인 데이터 추출/출력
        :grp1:err4:g/./;0\:;>(\().+\)>;#>.+(\))>+1p:err1:grp

[vrange]g![<Delim>][regex][<Delim>][cmd]
        반전 전역 명령어

[range]re[regex]
        정규식 키워드 설정

        range 는 텍스트를 이스케이프 처리해서 정규식으로 사용합니다.

        예시: 키워드를 “int” 로 설정
        :re int
		
        예시: 1행부터 3행까지 키워드로 설정
        :1,3re

[vrange]i[str]
        ex 삽입 모드 진입

        [str] 은 삽입 버퍼에 입력될 초기 값을 지정합니다.
        원시 ex 모드에서 [str] 은 대화형 모드를 거치지 않고 직접 삽입됩니다.

        예: vi/ex에서 “hello” 삽입
        :i hello<^M><ESC>
		
        예: vi/ex에서 <Newline> 을 제거하고 “hello” 삽입
        :i hello<^M><^M><Backspace>
		
        예: vi/ex 모드에서 변경 내용 취소
        :i hello<^M><^C>
		
        예: “hello” 즉시 삽입
        :i hello<^V><ESC>
		
        예: 지정된 위치 앞에 “hello” 삽입
        :-1i hello<^V><ESC>
		
        예: 1행 앞에 “hello” 삽입 (특수 사례)
        :0i hello<^V><ESC>
		
        예: 원시 ex 모드에서 “hello” 삽입
        i hello
		
        예: 원시 ex 모드에서 두 줄 삽입
        i hello<^M>world
		
        예: 원시 ex 모드에서 “hello” 삽입 (대화형)
        i<^M>hello<^M>.<^M>

[range]c[str]
        교체 모드 진입

        seq 및 lim ex 옵션과 함께 사용하면 이 명령어는 매우 긴 줄을 수정하는 데 가장 적합합니다.
        32비트 빌드에서 삽입 버퍼의 최대 용량은 약 1.33GB입니다.
        32비트 빌드에서 처리 가능한 최대 줄 길이는 2.0GB입니다.

        예시: 현재 줄을 “hello” 로 대체하기
        :c hello<^M><ESC>
		
        예시: 1~5행을 “hello” 로 바꾸기
        :1,5c hello<^M><ESC>
		
        예시: 현재 문자 위치에 “hello” 삽입하기
        :;c hello<^M><ESC>
		
        예시: 줄 끝에 “hello” 삽입하기
        :;$c hello<^M><ESC>
		
        예: 5번째 줄과 줄 끝에 “hello” 삽입
        :5;$c hello<^M><ESC>
		
        예: 현재 위치에서 5자 삭제
        :;;+5c<^M><ESC>

[range]d
        줄 삭제

        예: 현재 위치에서 버퍼 시작 부분까지 삭제
        :1,.; 0;d
		
        예: 현재 위치부터 버퍼 끝까지 삭제
        :.,$;;$d

[vrange]j[any]
        줄 병합

        어떤 인수를 지정해도 패딩 모드는 활성화됩니다.
        병합할 내용이 없으면 오류가 발생합니다.

        예: 모든 줄 병합
        :%-1j
		
        예: 공백으로 패딩해서 모든 줄 병합
        :%-1j x

[range]s[<Delim>][regex][<Delim>][str][<Delim>][<g>][<m>][<^>][#reg]
        치환 및 대체

        지정된 범위 내의 줄 중에서 괄호로 묶인 정규 표현식과 일치하는 텍스트를 괄호로 묶인 치환 문자열로 찾아 치환합니다.
        치환이 수행되지 않으면 오류를 반환합니다.

        예시: 범위 지정
        :1,5;5;5s/term1/term2/

        예시: 이전에 설정된 정규식 키워드와 일치하는 부분 교체
        :%s//term2/

        [str] 내의 대체 역참조는 \# 로 지정된 일치한 그룹의 텍스트를 삽입하며 여기서 #은 그룹 번호입니다. 
		숫자는 존재하는 가장 높은 그룹까지 탐욕적으로 소비되며 나머지는 문자 그대로 처리됩니다.

        예시: 대체 역참조. 
		
		이것은 대체에 대한 예시 텍스트이며 int 또는 void 가 포함되어 있습니다.
        :%s/(int)|(void)/pre\0after
		
        이것은 대체(sub)를 위한 예제 텍스트이며 preintafter 또는 void가 포함되어 있습니다
        :%s/(int)|(void)/pre\2after/g
		
        이것은 대체(sub)를 위한 예제 텍스트이며 prepreafterafter 또는 prevoidafter가 포함되어 있습니다

        예시: <g> 를 사용한 전역 대체
        :%s/term1/term2/g

        예시: <^> 를 사용해서 매 검색 시 앵커 ^ 적용 (이는 <g> 를 암시함)
        aaa
        :%s/^a/X/^
        XXX

        예시: 다중 행 모드 <m>
        :.,+1s/term1<^V><^M>term2/term3/m

        예시: 편집 레지스터 <a>를 그 자리에서 적용
        :s/term1/term2/97g

ud      되돌리기

        되돌릴 작업이 없으면 오류를 반환합니다.

rd      다시 실행

        다시 실행할 작업이 없으면 오류를 반환합니다.

[범위]p[문자열]
        버퍼의 한 줄 또는 여러 줄을 출력합니다.

        범위를 지정하지 않으면 left ex 옵션의 값에 따라 한 줄을 출력합니다.
        인수를 지정하면 평가된 인수를 출력합니다.

        예시: 문자 오프셋 범위 사용
        :1,10;5;5p
		
        예시: 교차된 문자 오프셋 범위
        :1;5,10;5p
		
        예시: 오프셋 5부터 10까지의 현재 줄 출력
        :.;5;10p
		
        예시: 버퍼 경로명 출력
        :p %

[range]=[<0-3 Any>][any]
        범위 번호를 출력합니다.

        인수 0-3 은 특정 번호를 선택합니다.
        <0-3> 이 아닌 인수나 추가 인수는 버퍼 범위 검사를 건너뜁니다.
        인수를 지정하지 않으면 모든 범위 번호를 출력합니다.

        예시: 현재 문자 오프셋만 출력
        :;= 2
		
        예시: 마크 <a>의 값 출력
        :'97=
		
        예시: 75 - 100 계산하기
        :,75-100=1p

=?[any]
        마지막 오류 출력

        어떤 인자라 하더라도 오류 상태를 전달합니다.

[range]m{#marks}
        줄 마크 설정

        마크는 공백으로 구분된 ID이며 범위의 시작과 끝을 번갈아 가면서 설정됩니다.

        예시: 나중에 :f> 에서 재사용할 수 있도록 <a> 및 <s> 에 범위를 저장
        :>top>,#>bottom>m 97 115:'97,'115f>middle

m![#marks]
        줄 마크 해제

        인수를 지정하지 않으면 현재 버퍼의 모든 마크는 해제됩니다.

[range]ya[#reg]
        레지스터로 복사

        인수를 지정하지 않으면 기본 레지스터로 복사됩니다.

        예: 범위 지정하여 <a> 레지스터로 복사
        :1,5;5;5ya 97

[range]ya+[#reg]
        복사한 내용을 레지스터에 추가

        예: 레지스터 <1>에 추가
        :ya+ 49

ya![#reg]
        레지스터 해제

[range]pu[#reg][any][\!{cmd}]
        레지스터 내용 붙여넣기 또는 파이프 전송

        예시: <a> 를 1행 5번째 위치에 붙여넣기
        :1;5pu 97
		
        예시: <a>를 1행 앞에 붙여넣기 (특수 사례)
        :0pu 97
		
        예시: 기본 레지스터를 X11 클립보드에 복사
        :pu \!xclip -selection clipboard
		
        예시: 레지스터 <a>를 X11 클립보드에 복사
        :pu 97 \!xclip -selection clipboard

[#reg]reg[str]
        레지스터 출력 또는 레지스터에 값 저장

        접두사는 레지스터를 선택하고 인수는 그 내용을 대체합니다.
        인수가 없는 접두사는 기본 레지스터를 재할당합니다.

        예: 화면 밖의 레지스터 부분을 볼 수 있도록 출력 위치를 이동
        :1left:reg
		
        예: 레지스터 <1>에 “hello” 저장
        :49reg hello

[#reg]reg+[str]
        레지스터를 출력하거나 레지스터에 내용을 추가합니다.

e[path]
        지정된 경로의 파일을 엽니다.

        인수를 지정하지 않으면 “이름 없는” 버퍼가 열립니다.
        32비트 빌드는 2.0GB보다 큰 파일은 잘립니다.
        첫 번째 널 종결자나 EOF가 나올 때까지 줄 바꿈이 이루어집니다.

e![path]
        지정된 경로의 파일을 강제로 엽니다.

        인수를 지정하지 않으면 파일 시스템에서 현재 버퍼를 다시 읽습니다.

[vrange]ef[regex]
        퍼지 검색 프롬프트로 파일을 엽니다.

        b-2 버퍼에 디렉터리 목록이 미리 채워져 있어야 합니다.

        예: :fd 를 사용해서 b-2 버퍼에 미리 채우기
        :fd
		
        예: find 를 사용해서 b-2 버퍼에 미리 채우기
        :b-2:%!find .

        프롬프트 동작은 :f 명령과 동일합니다.

        예: 프롬프트 입력 및 전체 목록 출력
        :ef
		
        예: “v”가 포함된 경로명 검색
        :ef v

[vrange]ef![regex]
        :ef 강제 실행 버전

[범위]r[경로]
[범위]r[\!{cmd}]
        파일 또는 파이프에서 읽기

        범위는 대상으로 계산됩니다.
        범위가 지정되지 않으면 <%> 범위로 처리됩니다.
        인수가 지정되지 않으면 현재 버퍼의 경로로 처리됩니다.

        예시: 파일 읽기
        :r vi.c
		
        예시: 모든 데이터를 파이프로 입력
        :r \!ls
		
        예시: 3, 5행만 파이프로 입력
        :3,5r \!ls

[범위]w[경로]
[범위]w[\!{명령}]
        파일 또는 파이프로 쓰기

        범위가 지정되지 않으면 <%> 범위로 평가됩니다.
        인수가 없으면 현재 버퍼 경로로 평가됩니다.

        예: 파일 쓰기
        :w vi.c
		
        예: 모든 데이터를 less로 파이프 전송
        :w \!less
		
        예: 처음 10줄만 파이프 전송
        :1,10w \!less

[범위]w![경로]
        파일 쓰기 강제 실행

[범위]wq[경로]
[범위]wq[\!{cmd}]
        파일을 쓰거나 파이프로 출력한 후 종료

[범위]wq![경로]
[범위]wq![\!{cmd}]
        파일을 강제로 쓰거나 파이프로 출력한 후 강제 종료

[범위]x[경로]
        저장하지 않은 변경 사항 저장 후 종료

[범위]x![경로]
        저장하지 않은 변경 사항 강제 저장 후 강제 종료

[#lvl]q[0-255]
        종료

        접두사를 사용하면 [#lvl] 개의 재귀 레벨을 종료합니다.
        인수는 셸 종료 코드를 지정합니다.

        예시: 현재 레벨과 그 위 1레벨을 조건부로 종료합니다.
        :%f>marker:??!p no marker\:1q:s/old/new/g:w

[#lvl]q![0-255]
        강제 종료

        접두사를 사용하면 [#lvl] 개의 재귀 레벨을 강제 종료합니다.
        접두사가 없으면 모든 재귀 레벨을 종료합니다.

        예시: 3단계 깊이의 레지스터 <#0> 을 “hello” 로 설정하고,
        이 레벨과 그 위 2개 레벨을 강제 종료하고 레지스터 내용을 외부로 출력
        :&Q&Q\\\:&Q0reg hello\\\\\\\:2q\\\\\\\!:p %@0

b[#index]
        버퍼를 출력하거나 버퍼 전환

        임시 버퍼는 메인 버퍼와 별도로 관리되며 음수 인덱스로 선택됩니다.

        예: 5번째 버퍼로 전환
        :b5
		
        예: /hist/ ex 히스토리 버퍼로 전환
        :b-1
		
        예: /fm/ 디렉터리 목록 버퍼로 전환
        :b-2
		
        예: /sc/ 스크래치 버퍼로 전환
        :b-3

bp[path]
        현재 버퍼 경로 설정

bs[any]
        현재 버퍼를 저장하도록 설정

        인수를 지정하면 실행 취소/다시 실행 기록을 초기화합니다.

bx[#]   허용되는 버퍼의 최대 개수 설정

        지정된 개수가 현재 사용 중인 버퍼 수보다 적은 경우 버퍼는 해제됩니다.
        인수를 지정하지 않으면 기본값인 10으로 재설정됩니다.

cd[path]
        작업 디렉터리 설정

        인수를 지정하지 않으면 현재 디렉터리가 삭제되거나 마운트 해제된 경우 다시 연결합니다.

        현재 열려 있는 버퍼의 파일 경로는 새로 설정된 작업 디렉터리를 반영하도록 자동 조정됩니다.

fp[path]
        보조 디렉터리 경로를 지우거나 설정합니다

fd[path]
        b-2 버퍼의 디렉터리 목록을 계산합니다

        인수를 지정하지 않으면 보조 디렉터리 또는 작업 디렉터리 경로를 사용합니다.

inc[정규식]
        :fd 계산에 정규식을 포함합니다.

        인수를 지정하지 않으면 필터는 비활성화됩니다.

        예시: 서브 모듈 디렉터리에서 .c 로 끝나는 파일만 포함
        :inc submodule.*\.c$
		
        예시: .git 및 submodule 폴더 제외
        :inc (^(?\:(?\!^\.git|^submodule).)+[^/]+$)

[범위]![명령어]
        외부 프로그램 실행

        범위가 지정되면 버퍼의 데이터가 외부 프로그램으로 파이프 처리됩니다. 
		그 결과로 생성된 stdout 및 stderr은 다시 버퍼로 파이프 처리되어 원래 범위의 내용을 대체합니다.
        쉘 종료 코드가 0이 아닐 경우나 범위가 지정되지 않은 경우 오류를 반환합니다.

        예시: 버퍼를 정렬하는 유명한 예시
        :%!sort
		
        예시: sed를 사용해서 “int” 를 “uint” 로 바꾸기
        :%!sed -e ‘s/int/uint/g’
		
        예시: 줄 교체없이 ls 명령 결과를 파이프로 전달하기
        :;$+1!ls
		
        예시: tr을 사용해서 현재 위치의 단어를 대문자로 바꾸기
        :;;>\\\>>!tr ‘[\:lower\:]’ '[\:upper\:]'

&{매크로}
        전역 비차단 매크로

        원시 vi 입력 시퀀스를 실행합니다.
        비차단 매크로는 시퀀스의 끝 부분에 도달했을 때 입력을 기다리지 않습니다. 
		다른 매크로를 실행하는 비차단 매크로는 항상 종료 지점에 도달합니다.

        예: vi 삽입 문장 실행
        :& ihello
		
        예: :hello 실행
        :& \:hello<^V><^M>
		
        예: vi “ci(int” 매크로 실행
        :& ci(int
		
        예: 비차단 매크로 내부에 차단 매크로 중첩
        :& \:@ \\\:blocking<^V><^M>i 비차단 상태로 계속
		
        예: vi 줄 번호 영구 활성화
        :& 2#

@{macro}
        전역 차단 매크로

        원시 vi 입력 시퀀스를 실행합니다.
        차단 매크로는 시퀀스 끝 부분에 도달하면 입력을 기다려야 합니다. 
		다른 매크로를 실행하는 차단 매크로는 입력 정체를 유발할 수 있습니다.

        예: vi 삽입 문장 실행
        :@ ihello
		
        예: <:> vi 프롬프트에 “hello” 삽입
        :@ \:hello
		
        예: vi “ci(int” 매크로 실행
        :@ ci(int
		
        예: 삽입 모드에서 빠져나오며 “ci(int” 실행
        :@ ci(int<^V><^C>
		
        예: 후속 명령으로 “ci)INT” 실행
        :@ ci(int<^V><^C>ci)INT
		
        예: 사용자가 삽입 모드를 종료한 후 vi “dw” 명령 실행
        :@i:@dw

[#count <$>]?[cond]
        While 루프

        [cond] 를 [#count] 번 반복하거나 [<$>] 를 사용해서 무한 반복합니다.
        [cond] 가 오류를 반환하거나 카운트가 초과되면 While 루프는 중단됩니다.
        [cond] 의 오류 상태가 전달됩니다.
        접두사가 없으면 [#count] 의 기본값은 1입니다.

        예시: 임의의 횟수를 사용해서 파일의 모든 줄을 병합
        :10000? & J
		
        예시: 모든 작업 취소
        :$? ud
		
        예시: 일련의 ex 명령을 10번 반복
        :10? 1p\:5p\:10p

[#count <$>]?![cond]
        반전 while 루프

[접두사]??[then]
{접두사}??
        조건문

        분기 여부는 마지막 명령어의 오류 상태에 따라 결정됩니다.
        [then] 의 오류 상태가 전달됩니다.
        접두사와 인수 모두 없을 경우 마지막 오류 상태가 전달됩니다.

        인수가 없는 접두사는 명령어 체인에서 해당 시점의 현재 오류 상태를 지정된 캡처 ID에 캡처합니다.
        캡처 ID는 모든 중첩된 명령어 컨텍스트에서 전역적으로 적용됩니다.

        인수가 있는 접두사는 각 참조된 ID의 가장 최근 캡처 값을 대상으로 DNF 형식의 부울 표현식을 기반으로 분기합니다.
        ID에 캡처 값이 없으면 분기를 건너뜁니다.

        접두사 연산자:
        숫자 캡처 ID
        , AND (우선순위 높음)
        ; OR (우선순위 낮음)

        예시: :cd가 성공한 경우만 디렉터리 계산
        :cd /blah:?? fd
		
        예시: 오프셋 5에 “int” 가 포함된 경우만 한 줄 출력
        :;5;#>^int>:?? p
		
        예시: vi.c 파일이 존재할 경우만 편집
        :![ -f ./vi.c ]:??e ./vi.c:mpt1
		
        예시: ID 5에서 오류 상태를 캡처하고 나중에 분기
        :>int>:5??:;=:5??p
		
        예시: ID가 설정된 적이 없으면 건너뛰기
        :2??.=
		
        예시: AND -- 두 ID 모두 성공을 캡처한 경우에만 분기
        :f>int:1??:f>void:2??:1,2??p 둘 다 발견됨
		
        예시: OR — 두 ID 중 하나라도 성공을 캡처한 경우 분기
        :f>int:1??:f>void:2??:1;2??p 하나 발견
		
        예시: DNF — (1 AND 2) OR (3 AND 4)
        :1,2;3,4??p 그 다음

[접두사]??![then]
{접두사}??!
        반전 조건문

        예시: 반전 캡처
        :f>int:1??!:1??p int가 없었습니다

{접두사}???
        캡처 ID 평가

        이것은 접두사에서 부울식을 평가하고 결과를 반환하는 무작위 명령어입니다.

        예시: while 루프 제어
        :$? \%f+int\:1??\:\%f+void\:2??\:1,2??p\:1,2???

[#id]?~[any]
        캡처 ID의 수명 제어

        접두사는 해제할 특정 ID를 선택합니다.
        인수와 접두사가 없으면 캡처된 모든 오류 상태가 해제됩니다.
        인수는 별도의 ex 명령어 체인 간에 캡처 상태 보존 여부를 전환합니다.

        예시: ID 2 캡처가 관련 없는 코드로 유출됨
        :f>int:2??:f>zzz:1??:1??f>int\:2??:2??p LEAKED
		
        예시: ID 2 유출 방지
        :f>int:2??:2?~:f>zzz:1??:1??f>int\:2??:2??p LEAKED

        예시: 다음 <:> 프롬프트에서 캡처를 사용
        :?~x:![ -f ./vi.c ]:1??  :1??p vi.c가 존재합니다

ft[파일 형식]
        파일 형식을 설정합니다.

        인수를 지정하지 않으면 현재 파일 형식을 출력합니다.
        하이라이트 ft 를 다시 불러오며 이를 통해 “hlw” 같은 옵션으로 생성된 동적 하이라이트를 재설정할 수 있습니다.

cm[키맵]
        키맵을 설정합니다.

        인수를 지정하지 않으면 현재 키맵 이름을 출력합니다.

cm![키맵]
        대체 키맵을 설정합니다.

ac[regex]
        자동 완성 필터 정규식을 설정합니다.

        인수를 지정하지 않으면 led.c 에 정의된 기본 단어 필터 정규식으로 재설정됩니다.

        예시: 버퍼의 전체 줄을 사용한 자동 완성
        :ac .+

[<0-3>]sc[<esc>][<sep>][<exp>][<exe>]
        ex 특수 문자를 설정합니다.

        접두사는 변경할 특정 문자를 선택합니다.
        인수가 없으면 기본값은 <\ : % !>입니다.

        예: <%>를 <a>로 변경
        :2sc a
		
        예: 단일 명령어 체인에서 새로 설정된 구분자를 활용
        :|1sc a|: p cmd1 a p cmd2 a|sc|a0?:p cmd3

[<0-3>]sc![<esc>][<sep>][<exp>][<exe>]
        ex 특수 문자 설정

        누락된 인수는 기본적으로 <#0> 으로 설정됩니다.

        예: <exp> 및 <exe> 비활성화
        :sc! \\\:

uc      
        멀티바이트 UTF-8 디코딩 토글

        이 명령은 인코딩이 혼합된 파일이나 바이너리 파일을 편집할 때, 또는 터미널이 UTF-8을 지원하지 않거나 
		UTF-8 문자를 표시하는 데 필요한 글꼴이 없을 때 특히 유용합니다. 
		이 명령은 종종 :ph 명령과 함께 사용해서 16진수 편집기 같은 기능을 구현합니다.

uz      
        제로 너비 문자 자리 표시자 토글

        제로 폭 문자를 자리 표시자로 렌더링하지 않고 숨깁니다.

ub      
        다중 코드포인트 시퀀스 자리 표시자 토글

        이모티콘 같은 다중 코드포인트 시퀀스를 자리 표시자로 대체하지 않고 터미널로 전달해서 원본그대로 렌더링합니다.

ph[#clow] [#chigh] [#width] [#blen][<Space>][str]
        자리 표시자 재정의

        이 명령은 conf.c 에 정의된 자리 표시자를 대체하며 이후의 :ph 명령은 자리 표시자 목록을 확장합니다.

        예: 8비트 ASCII(확장 ASCII)를 <~> 로 렌더링
        :ph 128 255 1 1~
		
        예: 완벽한 ISO/IEC 8859-1(latin-1) 지원
        :uc:ph 128 160 1 1~
		
        예: 제어 바이트 03 을 “^C” 로 표시
        :ph 3 3 2 1^C
		
        예: conf.c 의 기본값으로 재설정
        :ph
		
        예: conf.c 의 기본 자리 표시자 비활성화
        :ph:ph0
```

<\details>


<details><summary>EX OPTIONS<\summary>

ex 옵션은 전역 변수를 변경하기 위해 고안된 ex 명령어입니다. 변수는 부호 있는 정수로 저장됩니다. 인수는 값을 설정합니다. 숫자가 아닌 인수를 지정하면 첫 번째 바이트를 사용합니다. 별도로 명시되지 않는 한 인수를 지정하지 않으면 현재 값은 논리적으로 반전됩니다.

인수 표기법은 기본값이 표시됩니다.

```
ai[1]   새 줄 들여쓰기

        이 옵션은 최대 들여쓰기 길이의 배수입니다: 값 * 128

ic[1]   정규 표현식에서 대소문자 구분 무시

ish[0]  대화형 셸

        모든 <!> 명령을 대화형 셸로 실행합니다. 셸은 명령 실행 전에 .rc 파일을 불러옵니다.
        이를 통해 .rc 파일에 정의된 사전 정의된 함수, 별칭 및 ENV 변수를 사용할 수 있습니다.
        
        전제 조건 1:
                .rc 는 Bash의 .bashrc 나 Zsh의 .zshrc 같이 셸별 파일명을 의미합니다.
        전제 조건 2:
                환경 변수 $SHELL 이 기본 셸을 결정하며 그렇지 않은 경우 기본값은 /bin/sh 입니다.
        전제 조건 3:
                <!> 명령어가 예상된 결과를 반환하려면 .rc 파일에서 생성된 stdout 또는 stderr 출력이 없어야 합니다.

grp[0]  정규식 검색 그룹

        모든 정규식 검색 작업에 대한 대상 검색 그룹을 정의합니다. 
        이는 정규식 검색 결과가 기본 일치 그룹이 아닌 특정 그룹을 기반으로 처리할 때 필요합니다.

        예시: “int” 로 시작하는 문자 위치
        :grp 1:%f+int(.):grp

hl[1]   conf.c 에 정의된 규칙에 따라 텍스트를 강조 표시합니다.

        값이 1보다 크면 C 확장 기능만 사용해서 강조 표시합니다.

hlr[0]  텍스트를 반대 방향으로 강조 표시합니다.
hll[0]  파일 유형 hl에 따라 현재 줄을 강조 표시합니다.
hlp[0]  파일 유형 hl에 따라 “[]”, “()”, “{}” 쌍을 강조 표시합니다.
hlw[0]  파일 유형 hl에 따라 현재 단어를 강조 표시합니다.
led[1]  모든 터미널 출력을 활성화합니다.

vis[0]  시작 플래그 제어

        비트 1: 원시 ex 모드
        비트 2: ex 모드
        비트 3: :e 메시지 비활성화
        비트 4: 대체 터미널 화면

        예시: 원시 ex 모드 활성화
        :vis 3
		
        예시: 원시 ex 모드 비활성화
        :vis 2

mpt[0]  vi 프롬프트 제어

        vi에서 ex 명령이 호출된 후 이 값을 0 또는 1로 설정하면 “[계속하려면 아무 키나 누르세요]” 프롬프트가 비활성화됩니다.
        mpt가 음수인 경우 프롬프트는 비활성화된 상태로 유지됩니다.

        예시: 프롬프트와 마지막 출력 모두 비활성화
        :p:p:p:mpt 0
		
        예시: 프롬프트는 비활성화하되 마지막 출력은 유지
        :p:p:p:mpt 1

order[1]
        conf.c 에 정의된 규칙에 따라 문자 순서 재정렬

shape[1]
        아랍어 문자 모양 조정 수행

pac[0]  자동 완성 제안 사항을 실시간으로 출력
ts [8]  탭을 표시하는데 사용하는 공백 수
td[1]   현재 텍스트 방향 컨텍스트

        값  2: 전적으로 왼쪽에서 오른쪽으로
        값  1: conf.c 의 dctxs[] 를 따름, 기본값은 왼쪽에서 오른쪽으로
        값 -1: conf.c 의 dctxs[] 를 따름, 기본값은 오른쪽에서 왼쪽으로
        값 -2: 전적으로 오른쪽에서 왼쪽으로

pr[0]   레지스터 출력

        특정 레지스터에 문자나 숫자를 설정합니다.
        레지스터가 설정되면 이후 출력되는 모든 내용은 해당 레지스터에 저장됩니다.

        레지스터가 대문자로 설정된 경우 출력된 내용과 정확히 일치하도록 <줄바꿈> 문자가 추가됩니다.

        레지스터가 할당된 상태에서 R 또는 :reg 명령을 사용해서 프린트 레지스터의 내용을 표시할 수 없습니다(자기 참조 문제).

        예시: :b 명령과 똑같이 현재 버퍼 목록 붙여넣기
        :pr A:ya! 65: b:pu 65
		
        예시: :p 로 출력된 한 줄 저장하기
        :pr A:ya! 65:p

fr[0]   레지스터 찾기

        문자나 숫자로 레지스터를 설정합니다.
        레지스터가 설정되면, :f> 및 :f+ 명령은 현재 버퍼 대신 레지스터 내용을 대상으로 여러 줄에 걸친 앞으로 검색을 수행합니다. 
		범위는 일치한 부분을 현재 버퍼의 위치에 매핑합니다. 범위가 없으면 일치하는 부분을 그 자리에서 검사합니다.

        예시: 레지스터 <a>에서 “int” 검색
        :fr a:f>int:??p yes:??!p no:fr
		
        예시: “int” 뒤에 다음 줄에 “void”가 오는 부분 검색
        :%ya97:fr 97:%f>int.void:fr

rr[0]   레지스터 기록

        레지스터에 문자나 숫자를 설정합니다.
        레지스터가 설정되면 터미널에서 읽은 모든 바이트가 레지스터에 추가됩니다. 
		레지스터에서 재생되는 입력은 기록되지 않습니다.

        예: 레지스터 <a>에 키 입력을 기록
        :rr a
		
        예: 기록 중지
        :rr
		
        예: 레지스터 <a>에서 기록된 내용 재생
        :& %@97

lim[-1]
        줄 길이 렌더링 제한

        vi 모드는 커서가 위치한 줄을 제외한 모든 줄과,
        ex 모드는 모든 줄에 대한 렌더링 잘림 길이를 설정합니다.

        한 줄 또는 여러 개의 긴 줄(줄당 1MB 이상)이 포함된 파일을 편집할 때 다시그리기 성능을 향상시킵니다.

        예시: 적절한 값
        :lim 5000
		
        예시: 커서 위치의 줄만 렌더링
        :lim 0
		
        예시: 제한 비활성화
        :lim -1

seq[1]  실행 취소/다시 실행 제어

        seq 가 0일 경우 여러 개의 서로 다른 작업을 한 번에 취소하거나 다시 실행합니다.

        고부하 작업 시 메모리 사용량을 최적화하기 위해 seq 를 음수 값으로 설정해서 실행 취소/다시 실행 추적을 비활성화합니다.

        예: 실행 취소/다시 실행 작업을 단일 단계로 테스트
        :&ocontrol test:seq 0:&a step1:&a step2:&a step3:seq
		
        예: 실행 취소/다시 실행을 완전히 비활성화
        :seq -1

[hscroll]left[0]
        수평 스크롤 제어

        인수를 지정하지 않으면 현재 문자 오프셋을 사용해서 값을 계산합니다.
        hscroll 은 다음 공식으로 값을 계산합니다: xcols / 2 * hscroll

err[1]  ex 오류 제어

        비트 1: 오류 출력
        비트 2: 조기 종료
        비트 3: 오류 무시

        예시: 오류 메시지 출력 없이 오류 무시
        :err 4
		
        예시: 오류 발생 시 메시지 출력 후 첫 번째 오류 발생 시 종료
        :err 3
		
        예시: 오류 발생 시 메시지 출력하고 오류 상태 판단 시 마지막 명령어만 고려
        :err 1
```

<\details>


<details><summary>EXINIT ENV VAR<\summary>

`EXINIT` 환경 변수는 프로그램 시작 시 수행할 일련의 ex 명령어를 정의하며 모든 파일이 열린 이후 한 번만 실행됩니다. 

이는 C 언어나 유닉스 파이프를 제외하고 Nextvi를 스크립팅하거나 사용자 정의할 수 있는 **유일한 방법** 입니다. grep, awk, sed 같은 수많은 표준 텍스트 처리 유틸리티는 `EXINIT` 을 고려해서 Nextvi로 대체할 수 있습니다.

8진수 이스케이프 문자를 사용하는 `printf` 는 `EXINIT` 에 제어 문자를 삽입하는 표준적인 방법입니다.

**예시:**
```
        export EXINIT=‘td 2:order 0:lim 5000’
        긴 줄에 대한 처리 성능을 최적화하기 위해 ex 옵션을 설정합니다.

        export EXINIT=‘b-1:r ./vi.c:b-1’
        vi.c 파일을 히스토리 버퍼(b-1)에 불러옵니다.

        export EXINIT="$(printf ‘e ./vi.c:& i\007\003:bx 1:bx’)"
        자동 완성용 vi.c 인덱스 생성

        export EXINIT="$(printf ‘97reg o{\n}\003kA\n\t’)"
        레지스터 `<a>` 에 @ 매크로 설정

        @a 매크로는 커서 바로 아래에 `<{}` 와 닫는 `<}>` 를 생성하며 중괄호 사이에 커서를 삽입 모드로 남겨둡니다.

        export EXINIT=‘98reg \:mpt-1\:;;$w \\\!xclip -selection clipboard\:mpt’
        레지스터 `<b>` 에 @ 매크로 설정

        @b 매크로는 현재 줄을 X11 클립보드로 전송합니다.

        export EXINIT=‘rr z:vis 2:&Q:b-3:pu 122:wq! bugreport.txt’
        버그 보고를 위해 세션을 캡처합니다.

        레지스터 `<z>` 에 기록이 시작됩니다. `vis 2` 를 설정하면 `<Q>` 는 일반 vi 세션으로 전환되고 사용자가 제어할 수 있습니다. 
        `:q` 로 종료하면 매크로가 재개되고 기록 내용을 bugreport.txt 파일에 저장합니다.
```

<\details>


<details><summary>REGEX<\summary>

`Pikevm` 은 Nextvi를 위해 개발된 백트래킹 없는 초고속 NFA 시뮬레이션 정규식 엔진입니다. 이 엔진은 정규식이 상수 공간과 O(`n * k`) 시간 복잡도 내에서 평가되도록 보장하며 여기서 `<n>` 은 입력 문자열의 길이고 `<k>` 는 정규식의 구조적 복잡도(예: 상태 전환 또는 선택)를 의미합니다. 비록 이것이 엄격한 O(n) 선형 실행 시간 성능을 보장하진 않지만 계산 및 메모리 자원이 입력 전체에 걸쳐 선형적이고 균등하게 분배되도록 보장하며 `<k>` 는 상수 계수에 직접적인 영향을 미칩니다. 이 원리는 기수 정렬 알고리즘에서 활용되는 개념과 유사합니다.

Pikevm의 구문은 Plan 9의 `regexp(7)` 과 유사합니다. 이 섹션의 설명은 매뉴얼 페이지 표기법을 무시합니다.

```
.       임의의 단일 문자 일치
[N-M]   N에서 M까지 대체 범위 집합 일치
{N,M}   N에서 M회까지 일치
()      캡처 그룹
(?:)    비캡처 그룹
*       0회 이상 반복
+       1회 이상 반복
|       합집합, 대안 분기
?       1회 또는 0회 일치 (탐욕적)
??      1회 또는 0회 일치 (비탐욕적)
^       줄 시작 확인
$       줄 끝 확인
\<      단어 시작 확인
\>      단어 끝 확인
(?=)    양방향 선행 확인
(?!)    음방향 선행 확인
(?>)    양방향 후행 확인
(?<)    음방향 후행 확인
(?#)    바이트 단위의 후행 오프셋
```

룩어라운드 표현식으로 이 표현식이 없으면 구성할 수 없는 정규식을 만들 수 있습니다. 룩어라운드는 정규식 어디서나 사용할 수 있지만 몇 가지 주의 사항을 고려해야 합니다.

- 측면 1:
        룩어라운드는 완전한 정규식이 포함됩니다.
- 특징 2:
        룩어라운드는 중첩된 룩어라운드가 포함될 수 있습니다.
- 특징 3:
        `(?=^word)` 같은 정적 룩어라운드는 정규식 엔진을 우회하고 최적화됩니다.
- 특징 4:
        룩어라운드는 엔진에서 잠재적인 일치 항목을 좁혀 놓은 복잡한 패턴의 끝부분 근처에서 조건을 확인하는 데 가장 적합합니다.
- 특징 5:
        오프셋이 없거나 0인 룩비하인드는 이전 문자로부터 시작합니다. 오프셋이 지정된 경우, 현재 위치에서 지정된 값을 뺀 위치에서 스캔을 시작합니다. 음수 오프셋은 문자열 시작 부분에서 시작합니다.

<\details>


<details><summary>SPECIAL MARKS<\summary>

```
*       이전 명령어 체인 위치
[       이전 변경 사항 첫 번째 위치
]       이전 변경 사항 마지막 위치
'       이전 줄 영역 위치
`       이전 줄 영역 위치
```

<\details>


<details><summary>SPECIAL REGISTERS<\summary>

```
/       이전 정규식 키워드
:       이전 ex 명령어
<#0>    기본 레지스터
<0>     기본 레지스터의 이전 값 (원자형) ‘원자형’ 이란 연산에 <Newline> 이 포함되지 않았음을 의미
<1-9>   기본 레지스터의 이전 값(들) (비원자형)
```

<\details>


<details><summary>CODE MAP<\summary>


| 파일        | 설명           |
|-------------|----------------|
| 537 kmap.h  | 키맵 변환      |
| 582 vi.h    | 정의/보조      |
|-------------|----------------|
| 352 term.c  | 저수준 I/O     |
| 379 conf.c  | hl/ft/td 구성  |
| 462 ren.c   | 위치 지정/구문 |
| 615 uc.c    | UTF-8 지원     |
| 674 lbuf.c  | 파일/줄 버퍼   |
| 768 regex.c | pikevm         |
| 812 led.c   | 삽입 모드/출력 |
| 1907 vi.c   | 일반 모드/일반 |
| 2000 ex.c   | ex 옵션/명령어 |
| 7969 총계   | wc -l *.c 정렬 |


<\details>


<details><summary>COMPILING<\summary>

- `export CC=‘g++ -x c’`  
        컴파일러 설정, g++ 예시
- `export CFLAGS=‘-s’`  
        CFLAGS 설정, 스트립 예시
- `./cbuild.sh`  
        한 번 빌드
- `./cbuild.sh build`  
        빌드
- `./cbuild.sh debug`  
        심볼, 정적 분석, 동적 분석 포함 및 최적화 없음으로 빌드
- `./cbuild.sh pgobuild`  
        PGO 빌드는 일부 애플리케이션 특정 작업에서 상당한 성능 향상을 가져올 수 있습니다 
- `./cbuild.sh install`  
        vi를 $DESTDIR$PREFIX/bin에 설치
- `./cbuild.sh fetch`  
        업스트림 저장소의 커밋 병합
- `./cbuild.sh bench`  
        vi.c에서 2000단어 삭제 성능 벤치마크 테스트

<\details>


<details><summary>PHILOSOPHY<\summary>

대부분의 텍스트 편집기는 상당한 기술적 부채를 안고 있습니다. 이런 편집기들은 빠르게 익히거나 깊이 이해하기 어렵고 쉽게 변경할 수도 없습니다. Nextvi는 ‘기본성’, ‘간결성’, ‘유연성’ 이란 세 가지 개념을 중심으로 설계되었습니다. 

- ‘기본성’ 이란 무제한적인 텍스트 변환을 위한 완전하면서도 최소한의 명령어 세트를 의미합니다. 
- ‘간결성’ 이란 버스 팩터가 0인 간결하고 효율적이며 일관성 있는 구현을 의미합니다. 
- ‘유연성’ 이란 깊은 조합성과 다재다능하며 확장 가능한 C 코드베이스를 의미합니다. 

일반적인 상식으론 이 세 가지 개념은 모순된 것으로 여겨질 것입니다. 이 세 가지 개념 모두를 유지한다는 것은 텍스트 편집기에서 "하나를 희생해야만 다른 하나를 얻을 수 있다!" 는 통념에 도전한 것입니다. Nextvi는 이들의 공존이 가능할 뿐만 아니라 훌륭한 텍스트 편집기가 갖추어야 할 필수 요소임을 증명합니다.

> “모든 소프트웨어는 형편없지만, 어떤 것은 다른 것보다 더 형편없다.”
> 
> - 키릴 멜레킨

<\details>


<details><summary>SEE ALSO<\summary>

새로운 기능은 ‘patches’ 브랜치에 제공되는 선택적 패치로 이용할 수 있습니다. 의미있는 기여 내용이 있고 이를 공개하고 싶다면 이메일이나 GitHub 풀 리퀘스트를 통해 패치를 제출할 수 있습니다.
https://github.com/kyx0r/nextvi/tree/patches

- Patch2vi 지원 유틸리티
https://github.com/kyx0r/nextvi/tree/patch2vi

- 단위 테스트
https://github.com/kyx0r/nextvi/tree/test

- 이 설명서를 생성하는 데 사용된 스크립트
ㅠhttps://github.com/kyx0r/nextvi/tree/manual

- 통합 태그 릴리스
https://github.com/kyx0r/nextvi/tree/release

- 원본 Neatvi 저장소
https://github.com/aligrudi/neatvi

- Posix vi(1)
https://pubs.opengroup.org/onlinepubs/9699919799/utilities/vi.html

- Posix ex(1)
https://pubs.opengroup.org/onlinepubs/9699919799/utilities/ex.html

- Russ Cox의 NFA 정규 표현식
https://swtch.com/~rsc/regexp/regexp1.html

- Plan 9 regexp(7)
https://man.cat-v.org/p9p/7/regexp


<\details>


<details><summary>AUTHORS<\summary>

`Nextvi` 는 *Kyryl Melekhin <k.melekhin@gmail.com>* 이 작성했습니다. 이 프로그램은 *Ali Gholami Rudi <ali@rudi.ir>* 가 작성한 `neatvi(1)` 을 기반으로 합니다. 이 매뉴얼 페이지는 *nepeta <nepeta@canaglie.net>* 의 영감을 받아 작성되었습니다.

<\details>


**Nextvi 7.7                    September 19, 2026                    Nextvi 7.7**
